report_comment: >
  This report has been generated by the <a href="https://github.com/nf-core/hmfrnaseq/tree/dev" target="_blank">nf-core/hmfrnaseq</a>
  analysis pipeline.
report_section_order:
  "nf-core-hmfrnaseq-methods-description":
    order: -1000
  software_versions:
    order: -1001
  "nf-core-hmfrnaseq-summary":
    order: -1002

export_plots: true

disable_version_detection: true

# When using --replace-names, replace the entire sample name if the search
# string is found (not just the matching substring). This prevents partial
# matches from corrupting names that contain shorter sample IDs as substrings.
sample_names_replace_complete: true
