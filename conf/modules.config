/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Config file for defining DSL2 per module options and publishing paths
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Available keys to override module options:
        ext.args   = Additional arguments appended to command in module.
        ext.args2  = Second set of arguments appended to command in module (multi-tool modules).
        ext.args3  = Third set of arguments appended to command in module (multi-tool modules).
        ext.prefix = File name prefix for output files.
----------------------------------------------------------------------------------------
*/

process {

    withName: 'WRITE_REFERENCE_DATA' {
        def date = new java.util.Date().format('yyyyMMdd_HHmmss');
        publishDir = [
            path: { "${params.outdir}/reference_data/${workflow_version}/${date}" },
            mode: params.publish_dir_mode,
        ]
    }

    withName: 'STAR_GENOMEGENERATE' {
        label = 'process_high'
        ext.args = '--genomeSAindexNbases 14 --sjdbOverhang 200 --genomeChrBinNbits 15'
    }

    withName: 'GATK4_MARKDUPLICATES' {
        label = 'process_high'
        ext.args = '--CREATE_INDEX true'
        ext.prefix = { "${meta.id}.markdup.bam" }
        publishDir = [
            path: { "${params.outdir}" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : "${meta.key}/alignments/rna/${filename}" },
        ]
    }

    withName: 'REDUX' {
        label = 'process_high'
        publishDir = [
            path: { "${params.outdir}" },
            mode: params.publish_dir_mode,
            saveAs: { filename ->
                if (filename.equals('versions.yml') || filename.startsWith('.command.')) {
                    return null
                }
                return "${meta.key}/redux/${filename}"
            },
        ]
    }

    withName: 'ISOFOX' {
        label = 'process_high'
        publishDir = [
            path: { "${params.outdir}" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : "${meta.key}/${filename}" },
        ]
    }

    withName: 'READ_ALIGNMENT_RNA' {
        publishDir = [
            path: { "${params.outdir}/alignments/rna" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : "${meta.key}/${filename}" },
        ]
    }

    withName: 'RSEQC_BAMSTAT' {
        label = 'process_high'
        publishDir = [
            path: { "${params.outdir}/${meta.key}/rseqc/bamstat" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename },
        ]
    }

    withName: 'RSEQC_READDUPLICATION' {
        label = 'process_high'
        publishDir = [
            path: { "${params.outdir}/${meta.key}/rseqc/readduplication" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename },
        ]
    }

    withName: 'RSEQC_SPLITBAM' {
        label = 'process_high'
        publishDir = [
            path: { "${params.outdir}/${meta.key}/rseqc/splitbam" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename },
        ]
    }

    // FastQC output configuration - Fixed to follow subject_a pattern
    withName: 'FASTQC' {
        publishDir = [
            path: { "${params.outdir}/${meta.key}/fastqc" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> 
                if (filename.equals('versions.yml')) {
                    return null
                } else {
                    return filename
                }
            },
        ]
    }

    // MultiQC output configuration - per-sample reports in sample directories
    withName: 'MULTIQC' {
        publishDir = [
            path: { "${params.outdir}/${meta.key}/multiqc" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename },
        ]
        ext.args = '--comment "Quality control metrics for HMF RNA-seq pipeline"'
    }

    // Aggregated MultiQC report - all samples combined (no FastQC)
    withName: 'MULTIQC_AGGREGATED' {
        publishDir = [
            path: { "${params.outdir}/multiqc" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename },
        ]
        ext.args = '--comment "Aggregated quality control metrics across all samples"'
    }

    // Additional QC-related configurations
    withName: 'STAR_ALIGN' {
        label = 'process_high'
        publishDir = [
            path: { "${params.outdir}/${meta.key}/alignments/rna" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> 
                if (filename.equals('versions.yml')) {
                    return null
                } else if (filename.endsWith('Log.final.out')) {
                    return "star_logs/${filename}"
                } else {
                    return filename
                }
            },
        ]
    }

    withName: 'SAMTOOLS_SORT' {
        label = 'process_high'
        ext.prefix = { "${meta.prefix}" }
        publishDir = [
            path: { "${params.outdir}/${meta.key}/alignments/rna" },
            mode: params.publish_dir_mode,
            saveAs: { filename ->
                if (filename.equals('versions.yml')) {
                    return null
                } else {
                    return "sorted/${filename}"
                }
            },
        ]
    }

}