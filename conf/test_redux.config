/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for testing REDUX RNA workflow
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Full pipeline test with REDUX duplicate marking instead of GATK MarkDuplicates.
    Runs: STAR alignment -> REDUX -> RSeQC -> Isofox -> MultiQC

    Use as follows:
        nextflow run . -profile test_redux,docker --outdir <OUTDIR>
        nextflow run . -profile test_redux,singularity --outdir <OUTDIR>

----------------------------------------------------------------------------------------
*/

params {
    config_profile_name        = 'REDUX RNA Workflow Test profile'
    config_profile_description = 'Full RNA pipeline test with REDUX duplicate marking'

    // Input data
    input = params.pipelines_testdata_base_path + '/samplesheet/fastq_eval.subject_a.wgts.trna.minimal.csv'

    // Analysis config
    mode              = 'rna_redux_workflow'
    genome            = 'GRCh38_hmf'
    max_fastq_records = 0
}
